# 단체 여행자보험 신청 시스템 개발 계획서

## 📋 목차
1. [프로젝트 개요](#프로젝트-개요)
2. [개발 단계별 계획](#개발-단계별-계획)
3. [기술 스택](#기술-스택)
4. [데이터베이스 설계](#데이터베이스-설계)
5. [API 설계](#api-설계)
6. [프로젝트 구조](#프로젝트-구조)
7. [일정 계획](#일정-계획)
8. [우선순위](#우선순위)
9. [리스크 관리](#리스크-관리)

---

## 프로젝트 개요

### 프로젝트명
**단체 여행자보험 신청 및 관리 시스템 (Group Travel Insurance System)**

### 목적
단체 여행자보험 신청부터 계약 완료까지의 전체 프로세스를 디지털화하여 효율성을 향상시키고, 사용자 경험을 개선합니다.

### 주요 목표
- ✅ 프론트엔드와 백엔드 연동
- ✅ 실제 데이터베이스 연동
- ✅ 보안 강화 (주민번호 암호화, 인증 시스템)
- ✅ 실시간 신청 현황 관리
- ✅ 관리자 대시보드 구현

### 현재 상태
- ✅ 프론트엔드 UI 완료 (4개 페이지)
- ❌ 백엔드 미구현
- ❌ 데이터베이스 미구현
- ❌ API 미구현

---

## 개발 단계별 계획

### Phase 1: 기반 구축 (1-2주)

#### 1.1 프로젝트 초기 설정
- [ ] 프로젝트 폴더 구조 생성
- [ ] Git 저장소 초기화
- [ ] 개발 환경 설정 (Node.js, MySQL)
- [ ] 의존성 패키지 설치

#### 1.2 데이터베이스 설계 및 구축
- [ ] ERD 작성
- [ ] 테이블 생성 SQL 작성
- [ ] 데이터베이스 생성 및 테이블 생성
- [ ] 샘플 데이터 입력

#### 1.3 기본 백엔드 구조
- [ ] Express 서버 설정
- [ ] 데이터베이스 연결 설정
- [ ] 환경 변수 설정 (.env)
- [ ] 기본 미들웨어 설정 (CORS, body-parser, etc.)

---

### Phase 2: 핵심 기능 개발 (2-3주)

#### 2.1 인증 시스템
- [ ] 회원가입/로그인 API
- [ ] JWT 토큰 기반 인증
- [ ] 비밀번호 암호화 (bcrypt)
- [ ] 세션 관리
- [ ] 권한 관리 (단체 관리자, 취급자)

#### 2.2 단체 신청 기능
- [ ] 단체 신청 API (`POST /api/group/apply`)
- [ ] 고유 링크 생성 (UUID)
- [ ] 관리자 계정 자동 생성
- [ ] 이메일 발송 (관리자 정보, 링크)

#### 2.3 참가자 등록 기능
- [ ] 참가자 등록 API (`POST /api/participant/join`)
- [ ] 주민번호 검증 (서버 측)
- [ ] 주민번호 암호화 저장
- [ ] 중복 등록 방지

#### 2.4 참가자 조회/수정/취소
- [ ] 참가자 조회 API (`GET /api/participant/check`)
- [ ] 참가자 수정 API (`PUT /api/participant/update`)
- [ ] 참가자 취소 API (`DELETE /api/participant/cancel`)

---

### Phase 3: 관리자 기능 개발 (2주)

#### 3.1 단체 관리자 대시보드
- [ ] 대시보드 데이터 API (`GET /api/admin/dashboard`)
- [ ] 신청자 명단 조회 API (`GET /api/admin/list`)
- [ ] 엑셀 다운로드 API (`GET /api/admin/export`)
- [ ] 모집 마감 API (`POST /api/admin/close`)
- [ ] 링크 복사 기능

#### 3.2 취급자 관리 시스템
- [ ] 전체 신청 리스트 API (`GET /api/agent/list`)
- [ ] 신청 상세 조회 API (`GET /api/agent/detail/:id`)
- [ ] 상태 변경 API (`PUT /api/agent/status`)
- [ ] 메모 저장 API (`PUT /api/agent/memo`)
- [ ] 엑셀 다운로드 (전체 주민번호) API
- [ ] 문자 발송 API 연동

---

### Phase 4: 프론트엔드 연동 (1-2주)

#### 4.1 API 연동
- [ ] 단체 신청 페이지 연동
- [ ] 참가자 입력 페이지 연동
- [ ] 단체 관리자 페이지 연동
- [ ] 취급자 페이지 연동

#### 4.2 에러 처리
- [ ] 네트워크 오류 처리
- [ ] 유효성 검사 실패 처리
- [ ] 사용자 친화적 에러 메시지

#### 4.3 로딩 및 피드백
- [ ] 로딩 스피너 개선
- [ ] 성공/실패 토스트 메시지
- [ ] 폼 자동 저장 (draft)

---

### Phase 5: 보안 강화 (1주)

#### 5.1 데이터 보안
- [ ] 주민번호 암호화 (AES-256)
- [ ] HTTPS 적용
- [ ] SQL Injection 방지
- [ ] XSS 방지
- [ ] CSRF 토큰 적용

#### 5.2 접근 제어
- [ ] 권한별 접근 제어
- [ ] 세션 타임아웃
- [ ] 로그인 시도 제한
- [ ] 접근 로그 기록

---

### Phase 6: 테스트 및 최적화 (1주)

#### 6.1 테스트
- [ ] 단위 테스트
- [ ] 통합 테스트
- [ ] E2E 테스트
- [ ] 보안 테스트

#### 6.2 성능 최적화
- [ ] 데이터베이스 쿼리 최적화
- [ ] 인덱스 추가
- [ ] 캐싱 전략
- [ ] 이미지 최적화

#### 6.3 문서화
- [ ] API 문서 작성
- [ ] 사용자 매뉴얼
- [ ] 개발자 가이드

---

## 기술 스택

### Backend (제안)

#### 옵션 1: Node.js + Express (권장)
```json
{
  "runtime": "Node.js >= 16.0.0",
  "framework": "Express.js ^4.18.2",
  "database": "MySQL 8.0",
  "orm": "mysql2 (또는 Sequelize)",
  "authentication": "jsonwebtoken + bcryptjs",
  "validation": "express-validator + joi",
  "email": "nodemailer",
  "logging": "winston",
  "env": "dotenv"
}
```

**장점:**
- 기존 disk-cms 프로젝트와 동일한 스택
- JavaScript로 프론트/백엔드 통합 개발 가능
- 빠른 개발 속도

#### 옵션 2: PHP + Laravel
```json
{
  "language": "PHP >= 8.0",
  "framework": "Laravel 10.x",
  "database": "MySQL 8.0",
  "orm": "Eloquent",
  "authentication": "Laravel Sanctum",
  "validation": "Laravel Validator"
}
```

**장점:**
- 기존 pci0327 프로젝트와 동일한 스택
- 풍부한 기능 제공

**권장: Node.js + Express** (기존 프로젝트와의 일관성)

### Frontend
- **HTML/CSS**: 기존 파일 유지
- **JavaScript**: Vanilla JS (기존 코드 유지)
- **HTTP Client**: Fetch API 또는 Axios

### Database
- **RDBMS**: MySQL 8.0
- **문자셋**: utf8mb4
- **백업**: 자동 백업 설정

### Infrastructure
- **서버**: Linux (Ubuntu/CentOS)
- **웹서버**: Nginx (리버스 프록시)
- **프로세스 관리**: PM2
- **SSL**: Let's Encrypt

---

## 데이터베이스 설계

### ERD (Entity Relationship Diagram)

```
┌─────────────┐
│   groups    │ (단체)
└─────────────┘
      │
      │ 1:N
      │
      ▼
┌─────────────┐
│ participants│ (참가자)
└─────────────┘
      │
      │ N:1
      │
      ▼
┌─────────────┐
│   agents    │ (취급자)
└─────────────┘
```

### 테이블 구조

#### 1. groups (단체 정보)
```sql
CREATE TABLE groups (
    id INT PRIMARY KEY AUTO_INCREMENT,
    group_name VARCHAR(100) NOT NULL COMMENT '단체명',
    business_number VARCHAR(20) NULL COMMENT '사업자등록번호',
    destination VARCHAR(100) NOT NULL COMMENT '여행 목적지',
    purpose ENUM('워크숍', 'MT/OT', '체육대회', '일반여행', '수학여행') NOT NULL,
    start_date DATE NOT NULL COMMENT '시작일',
    end_date DATE NOT NULL COMMENT '종료일',
    manager_name VARCHAR(50) NOT NULL COMMENT '담당자 이름',
    manager_phone VARCHAR(20) NOT NULL COMMENT '담당자 연락처',
    manager_email VARCHAR(100) NOT NULL COMMENT '담당자 이메일',
    share_link VARCHAR(255) UNIQUE NOT NULL COMMENT '공유 링크 (UUID)',
    admin_id VARCHAR(100) UNIQUE NOT NULL COMMENT '관리자 아이디 (이메일)',
    admin_password VARCHAR(255) NOT NULL COMMENT '관리자 비밀번호 (암호화)',
    status ENUM('모집중', '마감됨', '견적요청', '입금대기', '계약완료') DEFAULT '모집중',
    total_participants INT DEFAULT 0 COMMENT '총 참가자 수',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_share_link (share_link),
    INDEX idx_status (status),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 2. participants (참가자 정보)
```sql
CREATE TABLE participants (
    id INT PRIMARY KEY AUTO_INCREMENT,
    group_id INT NOT NULL COMMENT '단체 ID',
    name VARCHAR(50) NOT NULL COMMENT '이름',
    rrn_encrypted TEXT NOT NULL COMMENT '주민등록번호 (암호화)',
    rrn_hash VARCHAR(255) NOT NULL COMMENT '주민번호 해시 (중복 체크용)',
    status ENUM('등록완료', '취소됨') DEFAULT '등록완료',
    agreed_at TIMESTAMP NULL COMMENT '동의 일시',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (group_id) REFERENCES groups(id) ON DELETE CASCADE,
    INDEX idx_group_id (group_id),
    INDEX idx_rrn_hash (rrn_hash),
    INDEX idx_status (status),
    UNIQUE KEY unique_group_rrn (group_id, rrn_hash)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 3. agents (취급자 정보)
```sql
CREATE TABLE agents (
    id INT PRIMARY KEY AUTO_INCREMENT,
    email VARCHAR(100) UNIQUE NOT NULL COMMENT '이메일 (아이디)',
    password VARCHAR(255) NOT NULL COMMENT '비밀번호 (암호화)',
    name VARCHAR(50) NOT NULL COMMENT '이름',
    department VARCHAR(50) NULL COMMENT '부서',
    role ENUM('취급자', '관리자') DEFAULT '취급자',
    is_active TINYINT(1) DEFAULT 1,
    last_login_at TIMESTAMP NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_email (email),
    INDEX idx_role (role)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 4. group_agent_memos (취급자 메모)
```sql
CREATE TABLE group_agent_memos (
    id INT PRIMARY KEY AUTO_INCREMENT,
    group_id INT NOT NULL,
    agent_id INT NOT NULL,
    memo TEXT NULL COMMENT '관리자 메모',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (group_id) REFERENCES groups(id) ON DELETE CASCADE,
    FOREIGN KEY (agent_id) REFERENCES agents(id) ON DELETE CASCADE,
    UNIQUE KEY unique_group_agent (group_id, agent_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

#### 5. audit_logs (감사 로그)
```sql
CREATE TABLE audit_logs (
    id INT PRIMARY KEY AUTO_INCREMENT,
    user_type ENUM('관리자', '취급자', '참가자') NOT NULL,
    user_id VARCHAR(100) NULL,
    action VARCHAR(50) NOT NULL COMMENT '액션 (등록, 수정, 삭제, 조회)',
    resource_type VARCHAR(50) NOT NULL COMMENT '리소스 타입 (group, participant)',
    resource_id INT NULL,
    ip_address VARCHAR(45) NULL,
    user_agent TEXT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_user_type (user_type),
    INDEX idx_action (action),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

## API 설계

### Base URL
```
Production: https://api.example.com
Development: http://localhost:3000
```

### 인증
모든 API는 JWT 토큰을 사용합니다 (참가자 조회 제외).

```
Authorization: Bearer <token>
```

### API 엔드포인트

#### 1. 단체 신청
```http
POST /api/group/apply
Content-Type: application/json

Request Body:
{
  "groupName": "OO대학교 학생회",
  "businessNumber": "1234567890",
  "destination": "제주도",
  "purpose": "워크숍",
  "startDate": "2025-07-15",
  "endDate": "2025-07-17",
  "managerName": "김철수",
  "managerPhone": "010-1234-5678",
  "managerEmail": "manager@example.com"
}

Response:
{
  "success": true,
  "data": {
    "groupId": 1,
    "shareLink": "https://insu.com/join/x7z9a2",
    "adminId": "manager@example.com",
    "adminPassword": "a1b2c3d4"
  }
}
```

#### 2. 참가자 등록
```http
POST /api/participant/join
Content-Type: application/json

Request Body:
{
  "shareLink": "x7z9a2",
  "name": "홍길동",
  "rrn": "990101-1234567"
}

Response:
{
  "success": true,
  "message": "등록이 완료되었습니다."
}
```

#### 3. 참가자 조회
```http
GET /api/participant/check?name=홍길동&rrn=990101-1234567

Response:
{
  "success": true,
  "data": {
    "id": 1,
    "name": "홍길동",
    "groupName": "OO대학교 학생회",
    "createdAt": "2025-12-02 14:30:00"
  }
}
```

#### 4. 참가자 수정
```http
PUT /api/participant/update/:id
Authorization: Bearer <token>

Request Body:
{
  "name": "홍길동",
  "rrn": "990101-1234567"
}
```

#### 5. 참가자 취소
```http
DELETE /api/participant/cancel/:id
Authorization: Bearer <token>
```

#### 6. 관리자 대시보드
```http
GET /api/admin/dashboard
Authorization: Bearer <token>

Response:
{
  "success": true,
  "data": {
    "totalParticipants": 24,
    "progress": 48,
    "shareLink": "https://insu.com/join/x7z9a2",
    "status": "모집중"
  }
}
```

#### 7. 신청자 명단
```http
GET /api/admin/list
Authorization: Bearer <token>

Response:
{
  "success": true,
  "data": [
    {
      "id": 24,
      "name": "김철수",
      "rrnMasked": "990101-1******",
      "createdAt": "2025.12.02 14:30",
      "status": "동의완료"
    }
  ]
}
```

#### 8. 엑셀 다운로드 (마스킹)
```http
GET /api/admin/export
Authorization: Bearer <token>

Response: CSV 파일 (주민번호 마스킹)
```

#### 9. 모집 마감
```http
POST /api/admin/close
Authorization: Bearer <token>

Request Body:
{
  "groupId": 1
}
```

#### 10. 취급자 리스트
```http
GET /api/agent/list?page=1&limit=20&status=신청완료
Authorization: Bearer <token>

Response:
{
  "success": true,
  "data": {
    "total": 142,
    "page": 1,
    "limit": 20,
    "items": [...]
  }
}
```

#### 11. 취급자 엑셀 다운로드 (전체)
```http
GET /api/agent/export/:groupId
Authorization: Bearer <token>

Response: CSV 파일 (전체 주민번호)
```

---

## 프로젝트 구조

```
jsy/
├── backend/
│   ├── config/
│   │   ├── database.js          # DB 설정
│   │   ├── jwt.js               # JWT 설정
│   │   └── email.js             # 이메일 설정
│   ├── controllers/
│   │   ├── groupController.js    # 단체 컨트롤러
│   │   ├── participantController.js
│   │   ├── adminController.js
│   │   └── agentController.js
│   ├── models/
│   │   ├── Group.js
│   │   ├── Participant.js
│   │   └── Agent.js
│   ├── routes/
│   │   ├── group.js
│   │   ├── participant.js
│   │   ├── admin.js
│   │   └── agent.js
│   ├── middleware/
│   │   ├── auth.js              # 인증 미들웨어
│   │   ├── validation.js        # 검증 미들웨어
│   │   └── errorHandler.js       # 에러 핸들러
│   ├── services/
│   │   ├── encryptionService.js # 암호화 서비스
│   │   ├── emailService.js      # 이메일 서비스
│   │   └── excelService.js      # 엑셀 서비스
│   ├── utils/
│   │   ├── logger.js
│   │   └── helpers.js
│   ├── .env.example
│   ├── server.js
│   └── package.json
├── frontend/
│   ├── 1. 단체가 최초 요청.html
│   ├── 2. 참가자들이 개인정보입력.html
│   ├── 3. 단체 어드민.html
│   ├── 4. 취급자 어드민.html
│   └── js/
│       ├── api.js               # API 호출 함수
│       ├── utils.js             # 유틸리티
│       └── validation.js        # 클라이언트 검증
├── database/
│   ├── schema.sql               # 테이블 생성
│   ├── seeds.sql                # 샘플 데이터
│   └── migrations/              # 마이그레이션
├── doc/
│   ├── 시스템_분석_문서.md
│   └── 개발_계획서.md
└── README.md
```

---

## 일정 계획

### 전체 일정: 8-10주

| Phase | 기간 | 주요 작업 | 담당자 |
|-------|------|----------|--------|
| Phase 1 | 1-2주 | 기반 구축 | 백엔드 개발자 |
| Phase 2 | 2-3주 | 핵심 기능 개발 | 백엔드 개발자 |
| Phase 3 | 2주 | 관리자 기능 | 백엔드 개발자 |
| Phase 4 | 1-2주 | 프론트엔드 연동 | 풀스택 개발자 |
| Phase 5 | 1주 | 보안 강화 | 백엔드 개발자 |
| Phase 6 | 1주 | 테스트 및 최적화 | 전체 팀 |

### 상세 일정

#### Week 1-2: 기반 구축
- Day 1-2: 프로젝트 설정, 폴더 구조
- Day 3-5: 데이터베이스 설계 및 구축
- Day 6-10: Express 서버 설정, 기본 구조

#### Week 3-5: 핵심 기능
- Week 3: 인증 시스템
- Week 4: 단체 신청, 참가자 등록
- Week 5: 참가자 조회/수정/취소

#### Week 6-7: 관리자 기능
- Week 6: 단체 관리자 대시보드
- Week 7: 취급자 관리 시스템

#### Week 8-9: 프론트엔드 연동
- Week 8: API 연동, 에러 처리
- Week 9: UI/UX 개선, 테스트

#### Week 10: 보안 및 최적화
- 보안 강화, 성능 최적화, 문서화

---

## 우선순위

### P0 (필수 - MVP)
1. ✅ 단체 신청 기능
2. ✅ 참가자 등록 기능
3. ✅ 관리자 대시보드 (명단 조회)
4. ✅ 기본 인증 시스템

### P1 (중요)
1. 참가자 조회/수정/취소
2. 엑셀 다운로드 (마스킹)
3. 모집 마감 기능
4. 취급자 리스트

### P2 (선택)
1. 취급자 상세 모달
2. 문자 발송 연동
3. 실시간 알림
4. 통계 대시보드

### P3 (향후)
1. 모바일 앱
2. 결제 시스템 연동
3. 증권 자동 발급
4. AI 챗봇

---

## 리스크 관리

### 기술적 리스크

| 리스크 | 영향도 | 대응 방안 |
|--------|--------|----------|
| 주민번호 암호화 성능 | 높음 | AES-256 하드웨어 가속 활용, 인덱스 최적화 |
| 동시 접속 처리 | 중간 | Connection Pool 설정, 로드 밸런싱 |
| 데이터 백업 | 높음 | 자동 백업 스크립트, 복구 테스트 |

### 일정 리스크

| 리스크 | 영향도 | 대응 방안 |
|--------|--------|----------|
| 개발 지연 | 중간 | 버퍼 시간 확보, 우선순위 조정 |
| 요구사항 변경 | 중간 | 변경 관리 프로세스, 문서화 |

### 보안 리스크

| 리스크 | 영향도 | 대응 방안 |
|--------|--------|----------|
| 개인정보 유출 | 매우 높음 | 암호화, 접근 제어, 감사 로그 |
| SQL Injection | 높음 | Prepared Statement, ORM 사용 |
| XSS 공격 | 높음 | 입력값 검증, 출력 인코딩 |

---

## 체크리스트

### 개발 전
- [ ] 프로젝트 요구사항 명확화
- [ ] 기술 스택 확정
- [ ] 개발 환경 구축
- [ ] 팀 역할 분담

### 개발 중
- [ ] 일일 스탠드업
- [ ] 주간 진행 상황 리뷰
- [ ] 코드 리뷰
- [ ] 테스트 작성

### 개발 후
- [ ] 통합 테스트
- [ ] 보안 점검
- [ ] 성능 테스트
- [ ] 사용자 테스트
- [ ] 문서화 완료

---

## 다음 단계

1. **기술 스택 확정**: Node.js + Express vs PHP + Laravel
2. **개발 환경 구축**: 로컬 개발 환경 설정
3. **데이터베이스 구축**: MySQL 설치 및 테이블 생성
4. **첫 번째 API 개발**: 단체 신청 API부터 시작

---

**작성일**: 2025년  
**버전**: 1.0  
**작성자**: 개발 계획팀

